<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dgk">

    <application
        android:name=".main.app.KApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Main子模块 -->
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Demo子模块 -->
        <activity android:name=".demo.DemoActivity" />
        <activity android:name=".demo.rxjava.RxJavaActivity" />
        <activity android:name=".demo.performance.NetworkActivity" />
        <activity android:name=".demo.service.ServiceActivity" />

        <service android:name=".demo.service.Service01" />

        <activity android:name=".demo.design.DesignActivity" />
        <activity android:name=".demo.design.component.BottomSheetActivity" />
        <activity android:name=".demo.design.component.ButtonActivity" />
        <activity android:name=".demo.design.component.CardActivity"/>


        <!-- Imitation子模块 -->
        <activity android:name=".imitation.ImitationActivity" />

        <!-- Note子模块 -->

    </application>

</manifest>
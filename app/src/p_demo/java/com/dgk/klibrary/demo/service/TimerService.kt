package com.dgk.klibrary.demo.service

import android.app.IntentService
import android.content.Intent

class TimerService(name: String) : IntentService(name) {

    override fun onHandleIntent(intent: Intent?) {

    }
}
